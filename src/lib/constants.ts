/**
 * Shared constants for DIA Live frontend.
 *
 * SECURITY: Admin PIN is NO LONGER exported from here.
 * It is verified server-side via Convex and stored in sessionStorage
 * after successful login. Use getAdminPin() from @/lib/adminSession.
 */

// Add other non-secret constants here as needed
